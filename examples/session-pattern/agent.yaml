apiVersion: forge.praxis-os.dev/v0
kind: AgentSpec
metadata:
  id: forge.examples.session-pattern
  version: 0.1.0
  displayName: "Session pattern demo"
provider:
  ref: provider.fake@1.0.0
prompt:
  system:
    ref: prompt.demo@1.0.0
    config:
      text: "You are a test agent for the session-pattern demo."
policies:
  - ref: policy.sensitivity-gate@1.0.0
